You are a long-running work session on the Theseus Midtown Madness 2 target. The launcher sets `RALPH_AGENT_PASSES` to the number of milestone passes expected in this session; it defaults to 8 and is at most 50. Do not stop after one blocker or one commit. Work through the available pass budget, committing progress as you go.

Read `AGENTS.md`, `doc/mm2.md`, and `doc/mm2-progress.md` first. The worktree is clean when the session starts. Never modify anything under `game/`, and never stage game files or generated artifacts.

For each milestone, reread `doc/mm2-progress.md`, choose the next smallest actionable blocker, reproduce it, make the smallest correct implementation change, and run the narrowest relevant formatter, check, test, or build. A failing build or test is useful diagnostic information, not a reason to end the session. Record the command, result, error count or next missing symbol, and next blocker in `doc/mm2-progress.md`. Then stage the milestone's tracked changes and create an unsigned commit with `git -c commit.gpgSign=false commit --no-gpg-sign`, using the standard Devin footer. Continue immediately with the next milestone after each commit.

Prefer shared compiler, runtime, and Win32 behavior over target-specific workarounds. Do not paper over unsupported behavior with broad stubs. Function overrides belong in `out/mm2/src/externs.rs` and must be wired through an explicit `tc --extern ADDRESS=NAME` argument. If an external prerequisite is unavailable, record the exact blocker, commit that progress, and continue with another independent backlog item when possible. Stop only when the pass budget is exhausted, no actionable work remains, or a fatal tooling or permission failure prevents further progress. Do not use `-S`, push, reset, or discard changes.
