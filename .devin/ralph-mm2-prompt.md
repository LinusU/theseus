Work as one bounded iteration on the Theseus Midtown Madness 2 target. Read `AGENTS.md`, `doc/mm2.md`, and `doc/mm2-progress.md`. Preserve all pre-existing working-tree changes. Never modify anything under `game/`. Choose exactly one smallest unfinished blocker, reproduce it, make the smallest correct implementation change, and run a focused formatter/check/test/build. Do not paper over unsupported behavior with broad stubs. Update `doc/mm2-progress.md` with the command, result, and next blocker. When the worktree was clean at the start, always stage this iteration's tracked changes and create an unsigned commit with `git -c commit.gpgSign=false commit --no-gpg-sign`, even when a build, test, or runtime check fails. Include the failure and next blocker in `doc/mm2-progress.md`; do not stage game files or generated artifacts. Use the standard Devin footer. Do not use `-S` or enable Git commit signing. If the iteration made no changes, no commit is needed. Do not push, reset, or discard changes. Stop after this one increment.
