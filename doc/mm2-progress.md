# Midtown Madness 2 progress

This is the durable handoff log for the MM2 agent loop. Each iteration should make one focused change, run a targeted check, and append a short entry here.

## Baseline

- Local input: `game/Midtown2.exe` and `game/MIDTOWN2.ICD` (ignored, user-provided)
- Format: PE32 Intel 80386 GUI executables
- `Midtown2.exe` is a loader; `MIDTOWN2.ICD` contains the main code but is encrypted on disk
- Translation input: a private decrypted PE image supplied through `MM2_INPUT`
- Translation entry point: `out/mm2/translate.sh`
- Loader hurdle: capture/reconstruct the decrypted ICD image with usable PE/import metadata
- Compiler baseline: the launcher scan found 99,382 blocks and about 93.6% code-section coverage
- Compiler blocker: `Float80` handling in `tc/src/codegen/mod.rs::mem_size` and x87 load/store codegen is resolved by the latest iteration
- Other compiler findings: two statically known jump targets were not discovered

## Ordered backlog

- [ ] Capture/reconstruct a decrypted ICD PE image from a compatible Windows runtime.
- [ ] Add an input validation/reconstruction tool or documented converter for the decrypted image.
- [x] Define and test correct handling for x87 `Float80` memory operands.
- [ ] Regenerate the decrypted MM2 image and fix the next compiler/code-generation failure.
- [ ] Compile the generated `mm2` crate and resolve generated Rust errors without masking unsupported behavior.
- [ ] Inventory static imports and implement only the missing Win32/DirectX/host behavior needed to reach startup.
- [ ] Run the translated program from `game/` and feed missing dynamic addresses back through `missing.txt`.
- [ ] Establish the first stable runtime milestone, then add and test selected Rust function overrides.
- [ ] Document a repeatable macOS run/debug workflow after the first successful launch.

## Iteration log

- Initial assessment: renamed the local installation to `game/`, added the root ignore rule, created the `out/mm2` target scaffold, and confirmed the translator reaches static discovery before failing on `Float80` code generation.
- Loader assessment: `Midtown2.exe` is a small loader and `MIDTOWN2.ICD` is a PE32 image with scrambled on-disk code. The target now requires a private decrypted/reconstructed ICD image through `MM2_INPUT`; `tc` accepts `.icd` inputs for that workflow.
- Float80 iteration: `MM2_INPUT=game/Midtown2.exe out/mm2/translate.sh` initially reproduced the `Float80` panic at `tc/src/codegen/mod.rs:190`; after adding 10-byte x87 extended-memory conversion and load/store codegen, the same command completed with 99,382 blocks. `rustfmt --edition 2024 --check runtime/src/fpu.rs runtime/src/lib.rs tc/src/codegen/mod.rs tc/src/codegen/fpu.rs && cargo check -p runtime -p tc && cargo test -p runtime -p tc` passed (10 tests). `cargo build --profile fast -p mm2` reached generated-crate compilation but failed on missing Win32 stdcall exports and unresolved `imm32`/`dplayx` modules. Next blocker: inventory and implement the missing generated Win32/DirectX imports needed for the `mm2` crate to compile.
- GetSystemDefaultLangID iteration: `cargo fmt --all -- --check` failed because the unrelated `out/winpin/src/generated.rs` module is absent; `rustfmt --edition 2024 --check win32/winapi/src/kernel32/nls.rs && cargo check -p winapi && cargo test -p winapi` passed (1 test). Added `kernel32::GetSystemDefaultLangID`, returning the US-English `LANGID` expected by the generated startup code. `cargo build --profile fast -p mm2` then failed with 232 errors (down from 234), with `user32::EnumDisplaySettingsA_stdcall` the first remaining missing export. Next blocker: implement `user32::EnumDisplaySettingsA` with the existing display-mode model rather than stubbing the generated call.
- EnumDisplaySettingsA iteration: `rustfmt --edition 2024 --check win32/winapi/src/user32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (1 test and zero doc-tests). Implemented `user32::EnumDisplaySettingsA` for current and registry settings, validating `DEVMODE.dmSize` and writing the existing 640x480x32/60Hz display mode. `cargo build --profile fast -p mm2` failed with 230 errors (down from 232); the first remaining missing export is `shell32::ShellExecuteA_stdcall`. Next blocker: implement `shell32::ShellExecuteA` with behavior appropriate for the translated startup path.
- ShellExecuteA iteration: `rustfmt --edition 2024 --check win32/winapi/src/shell32.rs && cargo check -p winapi && cargo test -p winapi` passed (1 test and zero doc-tests). Added `shell32::ShellExecuteA` with null-file failure reporting and an explicit no-association result for unsupported host shell execution, allowing the translated startup path to continue without a broad stub. `cargo build --profile fast -p mm2` still failed; `ShellExecuteA_stdcall` is resolved and `user32::ChangeDisplaySettingsA_stdcall` is now the first remaining missing export. Next blocker: implement `user32::ChangeDisplaySettingsA` against the existing display-mode model.
- ChangeDisplaySettingsA iteration: `rustfmt --edition 2024 --check win32/winapi/src/user32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (1 test and zero doc-tests). Added `user32::ChangeDisplaySettingsA` with restore handling, `DEVMODEA` validation for the supported 640x480 display model, 8/16/32-bit modes, 60 Hz, and existing-window resizing. `cargo build --profile fast -p mm2` failed with 224 errors (down from 230); `ChangeDisplaySettingsA_stdcall` is resolved and `kernel32::GlobalMemoryStatus_stdcall` is now the first remaining missing export. Next blocker: implement `kernel32::GlobalMemoryStatus` with the target's supported memory-status model.
- GlobalMemoryStatus iteration: `rustfmt --edition 2024 --check win32/winapi/src/kernel32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (1 test and zero doc-tests). Added the 32-byte `MEMORYSTATUS` ABI and `kernel32::GlobalMemoryStatus`, reporting the emulated flat memory capacity consistently for physical, page-file, and virtual fields. `cargo build --profile fast -p mm2` failed with 218 errors (down from 224); `GlobalMemoryStatus_stdcall` is resolved and `kernel32::GetDiskFreeSpaceA_stdcall` is now the first remaining missing export.
- GetDiskFreeSpaceA iteration: `rustfmt --edition 2024 --check win32/winapi/src/kernel32/misc.rs` passed after formatting, and `cargo check -p winapi && cargo test -p winapi` passed (1 test and zero doc-tests). Added `kernel32::GetDiskFreeSpaceA` with output-pointer validation and a 32 MiB free/64 MiB total emulated disk model. `cargo build --profile fast -p mm2` failed with 216 errors (down from 218); `GetDiskFreeSpaceA_stdcall` is resolved and `kernel32::GetFileAttributesA_stdcall` is now the first remaining missing export. Next blocker: implement `kernel32::GetFileAttributesA` against the existing filesystem model.
- GetFileAttributesA iteration: `rustfmt --edition 2024 --check win32/winapi/src/kernel32/file.rs`, `cargo check -p winapi`, and `cargo test -p winapi` passed (2 tests and zero doc-tests). Added `kernel32::GetFileAttributesA` using the existing Windows-path resolver and filesystem model, returning normal, directory, or invalid-file attributes. `cargo build --profile fast -p mm2` failed with 213 errors (down from 216); `GetFileAttributesA_stdcall` is resolved and `user32::GetWindowLongA_stdcall` is now the first remaining missing export. Next blocker: implement `user32::GetWindowLongA` against the existing window state.
- GetWindowLongA iteration: `rustfmt --edition 2024 --check win32/winapi/src/user32/window.rs`, `cargo check -p winapi`, and `cargo test -p winapi` passed (2 tests and zero doc-tests). Preserved the style and extended-style values in the emulated window and implemented `user32::GetWindowLongA` for `GWL_STYLE` and `GWL_EXSTYLE`, returning zero for unsupported indices or invalid handles. `cargo build --profile fast -p mm2` failed with 211 errors (down from 213); `GetWindowLongA_stdcall` is resolved and `user32::SetWindowLongA_stdcall` is now the first remaining missing export. Next blocker: implement `user32::SetWindowLongA` against the same window style state.
- SetWindowLongA iteration: `rustfmt --edition 2024 win32/winapi/src/user32/window.rs && rustfmt --edition 2024 --check win32/winapi/src/user32/window.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). Implemented `user32::SetWindowLongA` for `GWL_STYLE` and `GWL_EXSTYLE`, returning the previous value while updating the emulated window state and returning zero for unsupported indices or invalid handles. `cargo build --profile fast -p mm2` failed with 209 errors (down from 211); `SetWindowLongA_stdcall` is resolved and `user32::GetWindowRect_stdcall` is now the first remaining missing export. Next blocker: implement `user32::GetWindowRect` against the existing window geometry.
- GetWindowRect iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `user32::GetWindowRect_stdcall` export with 209 errors. Added `user32::GetWindowRect` to validate the emulated handle, return the tracked window geometry translated by its screen position, and report invalid output pointers through the existing `Ptr::write` result. `rustfmt --edition 2024 --check win32/winapi/src/user32/window.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` still failed with 207 errors; `GetWindowRect_stdcall` is resolved and `user32::IsWindow_stdcall` is now the first remaining missing export. Next blocker: implement `user32::IsWindow` against the existing emulated window handle.
- IsWindow iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `user32::IsWindow_stdcall` export with 207 errors. Added `user32::IsWindow` to report whether the handle matches the existing emulated window. `rustfmt --edition 2024 --check win32/winapi/src/user32/window.rs`, `cargo check -p winapi`, and `cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 205 errors; `IsWindow_stdcall` is resolved and `user32::SetForegroundWindow_stdcall` is now the first remaining missing export. Next blocker: implement `user32::SetForegroundWindow` against the existing single-window focus model.
- SetForegroundWindow iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `user32::SetForegroundWindow_stdcall` export with 205 errors. Added `user32::SetForegroundWindow` to validate the single emulated window handle and post its activation and focus messages. `rustfmt --edition 2024 --check win32/winapi/src/user32/window.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 201 errors; `SetForegroundWindow_stdcall` is resolved and `kernel32::IsDBCSLeadByte_stdcall` is now the first remaining missing export. Next blocker: implement `kernel32::IsDBCSLeadByte` against the existing code-page model.
- IsDBCSLeadByte iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `kernel32::IsDBCSLeadByte_stdcall` export with 201 errors. Added `kernel32::IsDBCSLeadByte`, returning false for every byte because the emulated active code page is single-byte Windows-1252. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/nls.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 189 errors; `IsDBCSLeadByte_stdcall` is resolved and `gdi32::CreateSolidBrush_stdcall` is now the first remaining missing export. Next blocker: implement `gdi32::CreateSolidBrush` using the existing GDI object model.
- CreateSolidBrush iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `gdi32::CreateSolidBrush_stdcall` export with 189 errors. Added `gdi32::CreateSolidBrush` to allocate a `Brush` object with the requested `COLORREF` through the existing GDI handle table. `rustfmt --edition 2024 --check win32/winapi/src/gdi32/object.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 186 errors; `CreateSolidBrush_stdcall` is resolved and `user32::UnregisterClassA_stdcall` is now the first remaining missing export. Next blocker: implement `user32::UnregisterClassA` against the existing registered-window-class state.
- UnregisterClassA iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `user32::UnregisterClassA_stdcall` export with 186 errors. Added `user32::UnregisterClassA` to remove the single registered window class from the existing state and report whether one was registered. `rustfmt --edition 2024 --check win32/winapi/src/user32/window.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 184 errors; `UnregisterClassA_stdcall` is resolved and `kernel32::QueryPerformanceCounter_stdcall` is now the first remaining missing export. Next blocker: implement the performance-counter pair using the host monotonic clock.
- QueryPerformance iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `kernel32::QueryPerformanceCounter_stdcall` export with 184 errors. Added `kernel32::QueryPerformanceCounter` and `QueryPerformanceFrequency`, exposing the host millisecond clock with a 1,000 Hz frequency and validating the output pointer. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/nls.rs win32/winapi/src/kernel32/time.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 180 errors; both performance-counter exports are resolved and `kernel32::DebugBreak_stdcall` is now the first remaining missing export. Next blocker: implement `kernel32::DebugBreak` without terminating the translated process.
- DebugBreak iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `kernel32::DebugBreak_stdcall` export with 180 errors. Added `kernel32::DebugBreak` as a continuation-preserving no-op because the native host has no translated debugger exception path. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 177 errors; `DebugBreak_stdcall` is resolved and `kernel32::CreateProcessA_stdcall` is now the first remaining missing export. Next blocker: implement `kernel32::CreateProcessA` with process-start behavior that matches the translated target.
- CreateProcessA iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `kernel32::CreateProcessA_stdcall` export with 177 errors. Added the `PROCESS_INFORMATION` ABI and an explicit `CreateProcessA` failure for unsupported child-process execution rather than manufacturing a process handle. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 173 errors; `CreateProcessA_stdcall` is resolved and `user32::SetActiveWindow_stdcall` is now the first remaining missing export. Next blocker: implement `user32::SetActiveWindow` against the existing single-window focus model.
- SetActiveWindow iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `user32::SetActiveWindow_stdcall` export with 173 errors. Added `user32::SetActiveWindow` to validate the single active window, post activation/focus messages, and return the previous active handle. `rustfmt --edition 2024 --check win32/winapi/src/user32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 170 errors; `SetActiveWindow_stdcall` is resolved and `user32::SendMessageA_stdcall` is now the first remaining missing export. Next blocker: implement ANSI `SendMessageA` through the existing synchronous window-procedure dispatch.
- SendMessageA iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `user32::SendMessageA_stdcall` export with 170 errors. Added ANSI and Unicode `SendMessage` dispatch through the registered window procedure, preserving its synchronous return value in EAX and rejecting unknown windows/classes. `rustfmt --edition 2024 --check win32/winapi/src/user32/message.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 168 errors; both `SendMessage` exports are resolved and `kernel32::GetStringTypeExA_stdcall` is now the first remaining missing export. Next blocker: implement `kernel32::GetStringTypeExA` using the existing ANSI classification path.
- GetStringTypeExA iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `kernel32::GetStringTypeExA_stdcall` export with 168 errors. Added `kernel32::GetStringTypeExA` as the locale-aware ABI wrapper over the existing ANSI character classification implementation. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/nls.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 166 errors; `GetStringTypeExA_stdcall` is resolved and `user32::CharPrevA_stdcall` is now the first remaining missing export. Next blocker: implement `user32::CharPrevA` for the active single-byte code page.
- CharPrevA iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `user32::CharPrevA_stdcall` export with 166 errors. Added `user32::CharPrevA`, stepping back one byte while never moving before the caller-provided string start for the active single-byte code page. `rustfmt --edition 2024 --check win32/winapi/src/user32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 164 errors; `CharPrevA_stdcall` is resolved and `kernel32::InitializeCriticalSection_stdcall` is now the first remaining missing export. Next blocker: implement the critical-section lifecycle against the existing host threading model.
- Critical-section and interlocked iteration: `cargo build --profile fast -p mm2` initially reproduced 164 errors, led by the missing `kernel32::InitializeCriticalSection_stdcall` export. Added recursive critical-section ownership with condition-variable waiting keyed by guest address, plus `InterlockedIncrement` and `InterlockedDecrement` read-modify-write behavior. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/thread.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 141 errors; the critical-section and interlocked exports are resolved and `kernel32::IsBadReadPtr_stdcall` is now the first remaining missing export. Next blocker: implement `kernel32::IsBadReadPtr` using the emulated memory bounds.
- IsBadReadPtr iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `kernel32::IsBadReadPtr_stdcall` export with 141 errors. Added `kernel32::IsBadReadPtr`, treating the null page, arithmetic overflow, and ranges beyond the emulated flat-memory capacity as invalid. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (2 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 138 errors; `IsBadReadPtr_stdcall` is resolved and `kernel32::GetDriveTypeA_stdcall` is now the first remaining missing export. Next blocker: implement `kernel32::GetDriveTypeA` against the existing filesystem model.
- GetDriveTypeA iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `kernel32::GetDriveTypeA_stdcall` export with 138 errors. Added `kernel32::GetDriveTypeA`, reporting the supported `C:` mapping as a fixed drive and rejecting null-root alternatives, non-`C:` drives, and missing roots. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/file.rs && cargo check -p winapi && cargo test -p winapi` passed (3 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 136 errors; `GetDriveTypeA_stdcall` is resolved and `winmm::mixerGetNumDevs_stdcall` is now the first remaining missing export. Next blocker: implement the WinMM mixer device-count behavior used by the generated target.
- mixerGetNumDevs iteration: `cargo build --profile fast -p mm2` initially reproduced 136 errors led by the missing `winmm::mixerGetNumDevs_stdcall` export. Added the dedicated `winmm::mixer` module and reported one emulated mixer device. `rustfmt --edition 2024 --check win32/winapi/src/winmm/mixer.rs win32/winapi/src/winmm/mod.rs && cargo check -p winapi && cargo test -p winapi` passed (3 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 131 errors; `mixerGetNumDevs_stdcall` is resolved and `winmm::mixerGetDevCapsA_stdcall` is now the first remaining missing export. Next blocker: implement mixer capability reporting for the supported emulated device.
- mixerGetDevCapsA iteration: `cargo build --profile fast -p mm2` initially reproduced 131 errors led by the missing `winmm::mixerGetDevCapsA_stdcall` export. Added the 0x30-byte `MIXERCAPSA` ABI and capability reporting for mixer device zero, including invalid-device and short-buffer results. `rustfmt --edition 2024 --check win32/winapi/src/winmm/mixer.rs && cargo check -p winapi && cargo test -p winapi` passed (3 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 127 errors; `mixerGetDevCapsA_stdcall` is resolved and `winmm::mixerOpen_stdcall` is now the first remaining missing export. Next blocker: implement mixer handle creation for the supported device.
- mixerOpen and mixerClose iteration: `cargo build --profile fast -p mm2` initially reproduced 127 errors led by the missing `winmm::mixerOpen_stdcall` export. Added a shared `kernel32::Object::Mixer` handle, implemented `mixerOpen` output-handle creation for device zero, and made `mixerClose` release only mixer handles. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/event.rs win32/winapi/src/winmm/mixer.rs && cargo check -p winapi && cargo test -p winapi` passed (3 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 99 errors; mixer open/close exports are resolved and `winmm::mixerGetLineInfoA_stdcall` is now the first remaining missing export. Next blocker: serialize the emulated mixer destination line.
- mixerGetLineInfoA iteration: `cargo build --profile fast -p mm2` initially reproduced 99 errors led by the missing `winmm::mixerGetLineInfoA_stdcall` export. Added the 0xA8-byte `MIXERLINEA` ABI and component-type destination-line reporting for the emulated speakers target, with mixer-handle and buffer validation. `rustfmt --edition 2024 --check win32/winapi/src/winmm/mixer.rs && cargo check -p winapi && cargo test -p winapi` passed (4 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 95 errors; `mixerGetLineInfoA_stdcall` is resolved and `winmm::mixerGetLineControlsA_stdcall` is now the first remaining missing export. Next blocker: serialize the mixer line's control descriptors.
- mixerGetLineControlsA iteration: `cargo build --profile fast -p mm2` initially reproduced 95 errors led by the missing `winmm::mixerGetLineControlsA_stdcall` export. Added the game's observed 0x94-byte `MIXERCONTROL` ABI and populated the single volume control requested by the generated path, including its ID, type, names, bounds, and metrics. `rustfmt --edition 2024 --check win32/winapi/src/winmm/mixer.rs && cargo check -p winapi && cargo test -p winapi` passed (5 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 91 errors; `mixerGetLineControlsA_stdcall` is resolved and `winmm::mixerGetControlDetailsA_stdcall` is now the first remaining missing export. Next blocker: implement mixer control detail reads for the emulated volume control.
- mixerGetControlDetailsA iteration: `cargo build --profile fast -p mm2` initially reproduced 91 errors led by the missing `winmm::mixerGetControlDetailsA_stdcall` export. Added validation for the 24-byte control-details descriptor and bounds-checked full-scale unsigned values for each requested channel. `rustfmt --edition 2024 --check win32/winapi/src/winmm/mixer.rs && cargo check -p winapi && cargo test -p winapi` passed (5 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 94 errors because additional generated export-table diagnostics became visible; `mixerGetControlDetailsA_stdcall` is resolved and `winmm::mixerSetControlDetails_stdcall` is now the first remaining missing export. Next blocker: implement mixer control detail writes for the same volume model.
- mixerSetControlDetails iteration: `cargo build --profile fast -p mm2` initially reproduced 94 errors led by the missing `winmm::mixerSetControlDetails_stdcall` export. Extended the shared mixer handle to retain two channel volumes, made detail reads return that state, and implemented bounds-checked detail writes for control ID 1. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/event.rs win32/winapi/src/winmm/mixer.rs && cargo check -p winapi && cargo test -p winapi` passed (5 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 91 errors; both mixer detail exports are resolved and `kernel32::lstrcmpA_stdcall` is now the first remaining missing export. Next blocker: implement ANSI string comparison using the existing code-page behavior.
- lstrcmpA iteration: `cargo build --profile fast -p mm2` initially reproduced 91 errors led by the missing `kernel32::lstrcmpA_stdcall` export. Added the ANSI bytewise comparison contract returning -1, 0, or 1. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/strings.rs && cargo check -p winapi && cargo test -p winapi` passed (5 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 94 errors because additional generated export-table diagnostics became visible; `lstrcmpA_stdcall` is resolved and `user32::GetKeyNameTextA_stdcall` is now the first remaining missing export. Next blocker: implement the keyboard-name lookup used by the generated input path.
- GetKeyNameTextA iteration: `cargo build --profile fast -p mm2` initially reproduced 94 errors led by the missing `user32::GetKeyNameTextA_stdcall` export. Added common set-1 scan-code names, extended-key distinctions, unknown-key handling, and bounded ANSI buffer writes. `rustfmt --edition 2024 --check win32/winapi/src/user32/input.rs && cargo check -p winapi && cargo test -p winapi` passed (6 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 92 errors; `GetKeyNameTextA_stdcall` is resolved and `user32::LoadStringA_stdcall` is now the first remaining missing export. Next blocker: implement ANSI resource-string loading against the existing resource model.
- LoadStringA iteration: `cargo build --profile fast -p mm2` initially reproduced 92 errors led by the missing `user32::LoadStringA_stdcall` export. Added ANSI decoding from the existing UTF-16 string-resource blocks, module validation, missing-resource handling, and bounded null-terminated output. `rustfmt --edition 2024 --check win32/winapi/src/user32/resource.rs && cargo check -p winapi && cargo test -p winapi` passed (7 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 89 errors; `LoadStringA_stdcall` is resolved and `gdi32::CreateFontA_stdcall` is now the first remaining missing export. Next blocker: implement GDI font handle creation for the generated text path.
- CreateFontA iteration: `cargo build --profile fast -p mm2` initially reproduced 89 errors led by the missing `gdi32::CreateFontA_stdcall` export. Added a tracked font object containing the requested metrics and face name, implemented the standard 14-argument ANSI font constructor, and allowed fonts to be selected into emulated DCs. `rustfmt --edition 2024 --check win32/winapi/src/gdi32/mod.rs win32/winapi/src/gdi32/object.rs win32/winapi/src/gdi32/dc.rs && cargo check -p winapi && cargo test -p winapi` passed (7 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 86 errors; both generated `CreateFontA` calls and its export-table entry are resolved, and `gdi32::GetTextExtentPoint32A_stdcall` is now the first remaining missing export. Next blocker: implement text extent measurement for the selected GDI font.
- GetTextExtentPoint32A iteration: `cargo build --profile fast -p mm2` initially reproduced 86 errors led by the missing `gdi32::GetTextExtentPoint32A_stdcall` export. Added the `SIZE` ABI and bounded ANSI text extent calculation from the selected font metrics, with default metrics for an unselected font. `rustfmt --edition 2024 --check win32/winapi/src/gdi32/dc.rs && cargo check -p winapi && cargo test -p winapi` passed (9 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 80 errors; `GetTextExtentPoint32A_stdcall` is resolved and `gdi32::SetBkMode_stdcall` is now the first remaining missing export. Next blocker: implement background-mode state on the emulated DC.
- SetBkMode iteration: `cargo build --profile fast -p mm2` initially reproduced 80 errors led by the missing `gdi32::SetBkMode_stdcall` export. Added opaque default background mode state to each emulated DC and implemented validation plus previous-mode returns for opaque and transparent modes. `rustfmt --edition 2024 --check win32/winapi/src/gdi32/dc.rs && cargo check -p winapi && cargo test -p winapi` passed (9 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 76 errors; `SetBkMode_stdcall` is resolved and `gdi32::SetTextColor_stdcall` is now the first remaining missing export. Next blocker: persist text color on the emulated DC.
- SetTextColor iteration: `cargo build --profile fast -p mm2` initially reproduced 76 errors led by the missing `gdi32::SetTextColor_stdcall` export. Added text-color state to each DC and implemented previous-color returns with `CLR_INVALID` for invalid handles. `rustfmt --edition 2024 --check win32/winapi/src/gdi32/dc.rs && cargo check -p winapi && cargo test -p winapi` passed (9 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 67 errors; `SetTextColor_stdcall` is resolved and `user32::DrawTextA_stdcall` is now the first remaining missing export. Next blocker: implement ANSI text layout through the existing window/DC model.
- DrawTextA iteration: `cargo build --profile fast -p mm2` initially reproduced 67 errors led by the missing `user32::DrawTextA_stdcall` export. Added bounded ANSI text measurement, newline handling, single-line layout, and `DT_CALCRECT` rectangle updates through the selected emulated DC. `rustfmt --edition 2024 --check win32/winapi/src/gdi32/dc.rs win32/winapi/src/user32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (9 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 60 errors; `DrawTextA_stdcall` is resolved and `gdi32::SetBkColor_stdcall` is now the first remaining missing export. Next blocker: persist background color on the emulated DC.
- SetBkColor iteration: `cargo build --profile fast -p mm2` initially reproduced 60 errors led by the missing `gdi32::SetBkColor_stdcall` export. Added the Windows default white background color to each DC and implemented previous-color returns with `CLR_INVALID` for invalid handles. `rustfmt --edition 2024 --check win32/winapi/src/gdi32/dc.rs && cargo check -p winapi && cargo test -p winapi` passed (9 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 56 errors; `SetBkColor_stdcall` is resolved and `gdi32::TextOutA_stdcall` is now the first remaining missing export. Next blocker: implement ANSI text output against the selected DC.
- TextOutA iteration: `cargo build --profile fast -p mm2` initially reproduced 56 errors led by the missing `gdi32::TextOutA_stdcall` export. Added bounded ANSI text output with selected-font metrics, background-mode handling, color state, clipping, and a small raster font for the emulated framebuffer. `rustfmt --edition 2024 --check win32/winapi/src/gdi32/dc.rs && cargo check -p winapi && cargo test -p winapi` passed (9 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 54 errors; `TextOutA_stdcall` is resolved and `gdi32::Rectangle_stdcall` is now the first remaining missing export. Next blocker: implement rectangle drawing on the emulated DC.
- Rectangle iteration: `cargo build --profile fast -p mm2` initially reproduced 54 errors led by the missing `gdi32::Rectangle_stdcall` export. Added default brush state, brush selection, clipped fill, and pen borders for `gdi32::Rectangle`. `rustfmt --edition 2024 --check win32/winapi/src/gdi32/dc.rs win32/winapi/src/gdi32/object.rs && cargo check -p winapi && cargo test -p winapi` passed (9 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 52 errors; `Rectangle_stdcall` is resolved and `kernel32::SetLastError_stdcall` is now the first remaining missing export. Next blocker: add thread-local last-error state for Win32 calls.
- SetLastError iteration: `cargo build --profile fast -p mm2` initially reproduced 52 errors led by the missing `kernel32::SetLastError_stdcall` export. Wired `GetLastError` and `SetLastError` to the existing per-thread TEB `LastErrorValue` field. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/misc.rs win32/winapi/src/kernel32/thread.rs && cargo check -p winapi && cargo test -p winapi` passed (9 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 46 errors; `SetLastError_stdcall` is resolved and `kernel32::GetSystemInfo_stdcall` is now the first remaining missing export. Next blocker: report the emulated processor and memory layout through `GetSystemInfo`.
- GetSystemInfo iteration: `cargo build --profile fast -p mm2` initially reproduced 46 errors led by the missing `kernel32::GetSystemInfo_stdcall` export. Added the 36-byte `SYSTEM_INFO` ABI and the single-processor x86 memory-layout model used by the emulated host. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (10 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 44 errors; `GetSystemInfo_stdcall` is resolved and `kernel32::GetLocalTime_stdcall` is now the first remaining missing export. Next blocker: implement local and system time serialization.
- System-time iteration: `cargo build --profile fast -p mm2` initially reproduced 44 errors led by the missing `kernel32::GetLocalTime_stdcall` export. Added the 16-byte `SYSTEMTIME` ABI and UTC-backed local/system calendar serialization. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/time.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 40 errors; both time exports are resolved and `kernel32::RaiseException_stdcall` is now the first remaining missing export. Next blocker: preserve translated control flow when the host has no structured-exception dispatcher.
- RaiseException iteration: `cargo build --profile fast -p mm2` initially reproduced 40 errors led by the missing `kernel32::RaiseException_stdcall` export. Added a diagnostic, continuation-preserving RaiseException path because the native runtime has no structured-exception dispatcher. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 38 errors; `RaiseException_stdcall` is resolved and `kernel32::IsBadWritePtr_stdcall` is now the first remaining missing export. Next blocker: validate write-pointer ranges against emulated memory bounds.
- IsBadWritePtr iteration: `cargo build --profile fast -p mm2` initially reproduced 38 errors led by the missing `kernel32::IsBadWritePtr_stdcall` export. Added write-range validation using the existing emulated memory-boundary model. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 33 errors; `IsBadWritePtr_stdcall` is resolved and `kernel32::GetExitCodeProcess_stdcall` is now the first remaining missing export. Next blocker: continue the generated Win32 process-status export inventory in the next session.
- GetExitCodeProcess iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `kernel32::GetExitCodeProcess_stdcall` export with 33 errors. Added `kernel32::GetExitCodeProcess` for the supported current-process pseudo-handle, returning `STILL_ACTIVE` and rejecting unsupported process handles. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/process.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 31 errors; `GetExitCodeProcess_stdcall` is resolved and `kernel32::SetUnhandledExceptionFilter_stdcall` is now the first remaining missing export. Next blocker: implement the unhandled-exception-filter registration behavior.
- SetUnhandledExceptionFilter iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `kernel32::SetUnhandledExceptionFilter_stdcall` export with 31 errors. Added process-scoped filter registration that returns the previous guest callback address and supports clearing it. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/misc.rs win32/winapi/src/kernel32/state.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 28 errors; `SetUnhandledExceptionFilter_stdcall` is resolved and `kernel32::IsBadCodePtr_stdcall` is now the first remaining missing export. Next blocker: validate code-pointer ranges against emulated memory.
- IsBadCodePtr iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `kernel32::IsBadCodePtr_stdcall` export with 28 errors. Added code-pointer validation through the existing flat-memory boundary model, matching the runtime's lack of page permissions. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 26 errors; `IsBadCodePtr_stdcall` is resolved and `kernel32::SetConsoleCtrlHandler_stdcall` is now the first remaining missing export. Next blocker: implement console-control-handler registration for the emulated process.
- SetConsoleCtrlHandler iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `kernel32::SetConsoleCtrlHandler_stdcall` export with 26 errors. Added per-process console-handler registration, duplicate suppression, removal, and reset behavior; no host signal delivery is synthesized. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/misc.rs win32/winapi/src/kernel32/state.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 24 errors; `SetConsoleCtrlHandler_stdcall` is resolved and `winmm::midiOutSetVolume_stdcall` is now the first remaining missing export. Next blocker: implement the WinMM MIDI output volume state.
- midiOutSetVolume iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `winmm::midiOutSetVolume_stdcall` export with 24 errors. Added WinMM MIDI output volume state keyed by the opaque output handle, preserving the requested 32-bit volume value and returning success. `rustfmt --edition 2024 --check win32/winapi/src/winmm/midi.rs win32/winapi/src/winmm/mod.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 21 errors; `midiOutSetVolume_stdcall` is resolved and `kernel32::CreateDirectoryA_stdcall` is now the first remaining missing export. Next blocker: implement directory creation against the existing filesystem model.
- CreateDirectoryA iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `kernel32::CreateDirectoryA_stdcall` export with 21 errors. Added shared native and wasm host-filesystem directory creation, then wired `kernel32::CreateDirectoryA` through the existing Windows-path resolver. `rustfmt --edition 2024 --check host/src/fs.rs win32/winapi/src/kernel32/file.rs && cargo check -p host -p winapi && cargo test -p host -p winapi` passed (11 WinAPI tests, zero host tests, zero doc-tests). `cargo check -p host --target wasm32-unknown-unknown` was blocked by the stable-channel `#![feature(stdarch_wasm_atomic_wait)]` requirement. `cargo build --profile fast -p mm2` failed with 19 errors; `CreateDirectoryA_stdcall` is resolved and `dsound::ordinal2_stdcall` is now the first remaining missing export. Next blocker: implement the second DirectSound ordinal export.
- DirectSound ordinal-2 iteration: `cargo build --profile fast -p mm2` initially reproduced `dsound::ordinal2_stdcall` as the first unresolved export with 19 errors. Added the standard ordinal-2 alias to `DirectSoundEnumerateA`. `rustfmt --edition 2024 --check win32/winapi/src/dsound.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 17 errors; `ordinal2_stdcall` is resolved and `kernel32::GetThreadPriority_stdcall` is now the first remaining missing export. Next blocker: implement thread-priority reads for the emulated thread model.
- GetThreadPriority iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `kernel32::GetThreadPriority_stdcall` export with 17 errors. Added per-thread priority state, normal-priority defaults, invalid-handle rejection, and validated `SetThreadPriority` updates. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/thread.rs win32/winapi/src/kernel32/state.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 16 errors; `GetThreadPriority_stdcall` is resolved and `kernel32::GetPriorityClass_stdcall` is now the first remaining missing export. Next blocker: implement process-priority reads for the current process.
- GetPriorityClass iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `kernel32::GetPriorityClass_stdcall` export with 16 errors. Added current-process priority-class state with normal-priority initialization, supported class validation, and invalid-handle rejection. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/process.rs win32/winapi/src/kernel32/state.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 15 errors; `GetPriorityClass_stdcall` is resolved and `winmm::joyGetNumDevs_stdcall` is now the first remaining missing export. Next blocker: report the emulated joystick device count.
- joyGetNumDevs iteration: `cargo build --profile fast -p mm2` initially reproduced the missing `winmm::joyGetNumDevs_stdcall` export with 15 errors. Added the WinMM joystick device-count export, reporting zero devices because no joystick backend is exposed by the host model. `rustfmt --edition 2024 --check win32/winapi/src/winmm/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 14 errors; `joyGetNumDevs_stdcall` is resolved and the missing `imm32` module is now the first remaining blocker. Next blocker: add the narrow IMM32 exports required by the generated target.
- IMM32 iteration: `cargo build --profile fast -p mm2` initially reproduced 14 errors from the missing `imm32` module and six required exports. Added `ImmGetContext`, `ImmNotifyIME`, `ImmGetDefaultIMEWnd`, `ImmSetCompositionWindow`, `ImmAssociateContext`, and `ImmDestroyContext` with explicit no-IME results. `rustfmt --edition 2024 --check win32/winapi/src/imm32.rs win32/winapi/src/lib.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests). `cargo build --profile fast -p mm2` failed with 2 errors, both unresolved `dplayx::ordinal1_stdcall` references. Next blocker: add the narrow DirectPlay ordinal required by the generated target.
- DirectPlay ordinal-1 iteration: `cargo build --profile fast -p mm2` initially reproduced 2 unresolved `dplayx::ordinal1_stdcall` references. Added the DirectPlay ordinal-1 entry point with its DirectPlayCreate-shaped ABI and an explicit `E_NOTIMPL` result because no DirectPlay backend or COM interface model exists. `rustfmt --edition 2024 --check win32/winapi/src/dplayx.rs win32/winapi/src/lib.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests). `cargo build --profile fast -p mm2` passed with six generated-code warnings and zero errors. No remaining compile blocker was reported in the configured 12-pass session.
- Bit-instruction iteration: `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` initially panicked at generated `Bts`/`Bt` blocks. Added shared `BT`, `BTS`, `BTR`, and `BTC` code generation, including register-indexed memory addressing, plus typed runtime flag/update helpers and regression tests. `rustfmt --edition 2024 runtime/src/ops/bit.rs runtime/src/ops/mod.rs tc/src/codegen/mod.rs tc/src/codegen/misc.rs && cargo test -p runtime -p tc` passed (10 runtime tests and 3 compiler tests); translation and `cargo build --profile fast -p mm2` also passed. The next runtime launch panicked in `kernel32::OutputDebugStringA`; next blocker: preserve debug-string behavior without aborting the translated process.
- OutputDebugStringA iteration: the translated launch reached `kernel32::OutputDebugStringA` after `cargo build --profile fast -p mm2`, then panicked at its `todo!`. Implemented null-safe ANSI debug-string logging. `rustfmt --edition 2024 win32/winapi/src/kernel32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests); the subsequent target build passed with six generated-code warnings, and the runtime reached generated `Fabs not implemented` in `part_24.rs`. Next blocker: implement x87 `FABS` code generation/runtime behavior.
- FABS iteration: the translated launch reached generated `Fabs not implemented` in `part_24.rs`. Added `FABS` code generation as an absolute-value update of `ST0` with a compiler regression test. `rustfmt --edition 2024 tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p tc && cargo check -p runtime` passed (4 compiler tests and zero runtime errors); translation and `cargo build --profile fast -p mm2` passed with six generated-code warnings. The next runtime launch reached generated `Rdtsc not implemented` in `part_47.rs`. Next blocker: implement `RDTSC` from a host monotonic clock.
- RDTSC iteration: the translated launch reached generated `Rdtsc not implemented` in `part_47.rs`. Added a shared monotonic `RDTSC` clock based on `std::time::Instant` and code generation that writes its 64-bit result to EDX:EAX, with a compiler regression test. `rustfmt --edition 2024 runtime/src/lib.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p runtime -p tc` passed (10 runtime tests and 5 compiler tests); translation and `cargo build --profile fast -p mm2` passed with six generated-code warnings. The next runtime launch panicked in `kernel32::WaitForSingleObject` while looking up a non-event handle. Next blocker: return the correct wait result for supported thread or mutex handles.
- WaitForSingleObject iteration: the translated launch panicked in `kernel32::WaitForSingleObject` when it received a non-event handle. Added immediate mutex success, timeout for emulated thread objects without lifecycle tracking, invalid-handle failure, and event timeout handling. `rustfmt --edition 2024 win32/winapi/src/kernel32/event.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-tests); `cargo build --profile fast -p mm2` passed with six generated-code warnings and the target run completed successfully. No `missing.txt` was produced, so there is no remaining dynamic-address blocker at this milestone.
- Workspace verification iteration: `cargo test --workspace` compiled the relevant crates but failed with 1 error because the intentionally untracked `out/winpin/src/generated.rs` module is absent. This is the existing generated-target limitation also seen by full-format checks; the MM2-specific build and run remain successful. Next blocker: continue with independent shared-code verification or target hardening without staging generated output.
- Broader workspace verification iteration: `cargo test --workspace --exclude winpin` failed with 2 missing external inputs: `win32/Windows.Win32.winmd` and `scratch/dos/sbaitso/BLASTER.DRV`. Neither is needed by MM2, and no generated or game artifact was added. Next blocker: run the narrow relevant package suite and audit only target-relevant warnings.
- MM2 package-suite iteration: `cargo test -p runtime -p tc -p winapi -p host -p mm2` passed all 10 runtime, 5 compiler, and 11 WinAPI tests with zero failures or doc-test failures; host and MM2 contain no tests. The generated MM2 crate still emits six harmless self-assignment warnings. Next blocker: audit the shared compiler's remaining reachable-unimplemented instruction paths without changing generated output.
- FCOMPP iteration: the generated audit found many unimplemented x87 blocks, with `Fcompp` the most frequent exact instruction. Added `FCOMPP` comparison of ST0/ST1 followed by both stack pops and a compiler regression test. `rustfmt --edition 2024 tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p tc -p runtime` passed (6 compiler tests and 10 runtime tests); translation, target build, and target run all passed with six generated-code warnings. Next static compiler blocker: implement integer x87 subtraction `FISUB`.
- FISUB iteration: static generated output contained `Fisub not implemented` blocks. Added signed 16/32-bit integer subtraction from ST0 and a compiler regression test. `rustfmt --edition 2024 tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p tc -p runtime` passed (7 compiler tests and 10 runtime tests); translation, target build, and target run passed with six generated-code warnings. The `Fisub` panic paths are gone from generated output; next static compiler blocker: implement integer x87 addition `FIADD`.
- FIADD iteration: static generated output contained four `Fiadd not implemented` blocks. Added signed integer addition to the existing shared integer-x87 path and a compiler regression test. `rustfmt --edition 2024 tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p tc -p runtime` passed (8 compiler tests and 10 runtime tests); translation, target build, and target run passed with six generated-code warnings. Both `Fiadd` and `Fisub` panic paths are gone from generated output. Next static compiler blocker: implement integer x87 division, including reverse division.
- FIDIV iteration: static generated output contained 74 integer-x87 division panic paths across `Fidiv` and `Fidivr`. Added signed integer division and reverse division code generation with compiler coverage for both encodings. `rustfmt --edition 2024 tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p tc -p runtime` passed (9 compiler tests and 10 runtime tests); translation, `cargo build --profile fast -p mm2`, and the target run all passed with six generated-code warnings. The generated `Fidiv`/`Fidivr` panic paths are gone; remaining static x87 paths include `Fscale`, `Frndint`, and transcendental instructions.
- FSCALE iteration: static generated output contained five `Fscale not implemented` paths. Added shared `FSCALE` code generation using `ST0 * 2^trunc(ST1)` and a decoder/codegen regression test. `rustfmt --edition 2024 tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p tc` passed (10 tests and zero doc-test failures); translation, `cargo build --profile fast -p mm2`, and `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` all passed, with six generated-code warnings and no `missing.txt`. The next static x87 blocker is `Frndint`.
- FRNDINT iteration: static generated output contained five `Frndint not implemented` paths. Added `FRNDINT` code generation through the runtime FPU rounding helper and a decoder/codegen regression test. `rustfmt --edition 2024 tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p tc && cargo test -p runtime` passed (11 compiler tests, 10 runtime tests, and zero doc-test failures); translation, `cargo build --profile fast -p mm2`, and `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` all passed with six generated-code warnings and no `missing.txt`. The next static x87 blocker is `Fldl2e`.
- FLDL2E iteration: static generated output contained one `Fldl2e not implemented` path. Added the `FLDL2E` x87 constant push using `std::f64::consts::LOG2_E` and a decoder/codegen regression test. `rustfmt --edition 2024 tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p tc && cargo test -p runtime` passed (12 compiler tests, 10 runtime tests, and zero doc-test failures); translation, `cargo build --profile fast -p mm2`, and `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` all passed with six generated-code warnings and no `missing.txt`. The next static x87 blocker is `Fldpi`.
- FLDPI iteration: static generated output contained two `Fldpi not implemented` paths. Added the `FLDPI` x87 constant push using `std::f64::consts::PI` and a decoder/codegen regression test. `rustfmt --edition 2024 tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p tc && cargo test -p runtime` passed (13 compiler tests, 10 runtime tests, and zero doc-test failures); translation, `cargo build --profile fast -p mm2`, and `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` all passed with six generated-code warnings and no `missing.txt`. The next static x87 blocker is `Fldlg2`.
- FLDLG2 iteration: static generated output contained one `Fldlg2 not implemented` path. Added the `FLDLG2` x87 constant push using `std::f64::consts::LOG10_2` and a decoder/codegen regression test. `rustfmt --edition 2024 tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p tc && cargo test -p runtime` passed (14 compiler tests, 10 runtime tests, and zero doc-test failures); translation, `cargo build --profile fast -p mm2`, and `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` all passed with six generated-code warnings and no `missing.txt`. The next static x87 blocker is `Ftst`.
- FTST iteration: static generated output contained three `Ftst not implemented` paths. Added `FTST` comparison of ST0 against zero through the shared FPU comparison status and a decoder/codegen regression test. `rustfmt --edition 2024 tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p tc && cargo test -p runtime` passed (15 compiler tests, 10 runtime tests, and zero doc-test failures); translation, `cargo build --profile fast -p mm2`, and `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` all passed with six generated-code warnings and no `missing.txt`. The next static x87 blocker is `Fxam`.
- FYL2X iteration: static generated output contained two `Fyl2x not implemented` paths. Added `FYL2X` as `ST1 * log2(ST0)` followed by the required x87 stack pop, with a decoder/codegen regression test. The initial test attempt used `DE F1` (FDIVP) and failed 1 of 16 compiler tests; correcting it to the `D9 F1` `FYL2X` encoding made `rustfmt --edition 2024 tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p tc && cargo test -p runtime` pass (16 compiler tests, 10 runtime tests, and zero doc-test failures). Translation, `cargo build --profile fast -p mm2`, and `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` then passed with six generated-code warnings and no `missing.txt`. The next static x87 blocker is `Fxam`.
- FXAM iteration: the generated audit initially showed two `Fxam not implemented` paths; adding shared x87 condition-code state and `FPU::examine` exposed a third path in the same routine when translation continued. All three now generate `examine()`, which classifies normal, zero, infinity, NaN, and sign bits. The first `rustfmt --edition 2024 runtime/src/fpu.rs tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p runtime && cargo test -p tc` run failed 1 of 11 runtime tests because the assertion included TOP bits in the condition-code mask; after narrowing the mask, the same command passed (11 runtime tests, 17 compiler tests, and zero doc-test failures). Translation, `cargo build --profile fast -p mm2`, and `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed with six generated-code warnings and no `missing.txt`. The next static blocker is x87 `F2xm1`.
- F2XM1 iteration: static generated output contained two `F2xm1 not implemented` paths. Added `F2XM1` as the shared x87 `2^ST0 - 1` operation with a decoder/codegen regression test. `rustfmt --edition 2024 tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p tc` passed (18 tests and zero doc-test failures); translation, `cargo build --profile fast -p mm2`, and `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed with six generated-code warnings and no `missing.txt`. The next smallest actionable blocker is x87 `Fptan`.
- FPTAN iteration: static generated output contained four `Fptan not implemented` paths. Added `FPTAN` as `tan(ST0)` followed by the required push of `1.0`, with a decoder/codegen regression test. `rustfmt --edition 2024 tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p tc` passed (19 tests and zero doc-test failures); translation, `cargo build --profile fast -p mm2`, and `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed with six generated-code warnings and no `missing.txt`. The next static blocker is `Fnstenv`.
- FNSTENV iteration: the generated output contained one `Fnstenv not implemented` path. Added shared 28-byte x87 environment serialization for control/status/TOP, tag word, and the currently untracked pointer fields, plus a decoder/codegen regression test. `rustfmt --edition 2024 runtime/src/fpu.rs tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p runtime && cargo test -p tc` passed (11 runtime tests, 20 compiler tests, and zero doc-test failures); translation, `cargo build --profile fast -p mm2`, and `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed with six generated-code warnings and no `missing.txt`. Translation then exposed `Fldenv` as the next blocker.
- FLDENV iteration: the generated output contained one `Fldenv not implemented` path after `FNSTENV`. Added restoration of the x87 control word, condition codes, and TOP field from the shared 28-byte environment layout, with a decoder/codegen regression test. `rustfmt --edition 2024 runtime/src/fpu.rs tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p runtime && cargo test -p tc` passed (11 runtime tests, 21 compiler tests, and zero doc-test failures); translation, `cargo build --profile fast -p mm2`, and `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed with six generated-code warnings and no `missing.txt`. Thirteen static generated panic paths remain, led by the `Fnsave`/`Frstor` environment pair and unrelated control-flow/I/O instructions; the bounded 12-pass session is complete.
- Decrypted-input prerequisite iteration: `MM2_INPUT=scratch/mm2/MIDTOWN2.decrypted.exe out/mm2/translate.sh` failed immediately because the private decrypted PE input is absent, so the checked-in generated output cannot be refreshed. The existing generated target still builds and runs successfully with six warnings and no dynamic missing-address log. Next blocker: audit and cover the remaining shared compiler instruction paths using the available generated snapshot without modifying generated output.
- FNSAVE/FRSTOR iteration: the static generated audit identified the x87 environment-and-register save/restore pair as the next unsupported instructions. Added 108-byte x87 state serialization using the existing 28-byte environment layout plus eight 80-bit register slots, reset semantics for `FNSAVE`, and compiler/runtime regression coverage. `MM2_INPUT=scratch/mm2/MIDTOWN2.decrypted.exe out/mm2/translate.sh` was blocked because the private decrypted input is unavailable; `rustfmt --edition 2024 runtime/src/fpu.rs tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p runtime -p tc` passed (12 runtime tests, 22 compiler tests, zero doc-test failures). The existing generated target still contains the old two panic paths because it cannot be regenerated without `MM2_INPUT`; next blocker: implement `JO`/`JNO`/`JP`/`JNP` shared conditional flow.
- Overflow/parity jump iteration: the existing generated audit listed `Jo`, `Jno`, `Jp`, and `Jnp` as unsupported control-flow instructions. Added flag-based continuation selection in the shared runtime and included all four mnemonics in compiler control-flow generation; `rustfmt --edition 2024 runtime/src/ops/control_flow.rs tc/src/codegen/control_flow.rs && cargo test -p runtime -p tc` passed (13 runtime tests, 22 compiler tests, zero doc-test failures). `cargo build --profile fast -p mm2 && THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed with six generated-code warnings and exit code 0. The private `MM2_INPUT` remains unavailable for refreshing ignored generated output; next blocker: map the x86 `SAL` alias to the existing shared `SHL` implementation.
- SAL iteration: the generated audit contained one `Sal not implemented` path. Mapped iced-x86's `SAL` mnemonic to the existing shared `SHL` code-generation function and added decoder coverage; `rustfmt --edition 2024 tc/src/codegen/math.rs tc/src/codegen/mod.rs && cargo test -p tc -p runtime` passed (23 compiler tests, 13 runtime tests, zero doc-test failures). `cargo build --profile fast -p mm2 && THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed with six generated-code warnings and exit code 0. The private decrypted input is still unavailable, so ignored generated output cannot be refreshed; next blocker: handle the remaining `INT1` trap without aborting translated control flow.
- INT1 iteration: the static generated audit contained one `int1` debug-trap path. Treated `INT1` as a continuation-preserving no-op in shared code generation, matching the runtime's existing lack of a translated debugger exception dispatcher, and added decoder coverage. `rustfmt --edition 2024 tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p tc -p runtime` passed (24 compiler tests, 13 runtime tests, zero doc-test failures). The private decrypted input is unavailable, so target translation was not rerun; next static compiler blocker: handle `INT3` with the same explicit debugger-trap policy.
- INT3 iteration: the generated audit contained an `int 3` debugger-trap path. Applied the same explicit continuation-preserving policy to the dedicated `INT3` opcode and added decoder coverage; `rustfmt --edition 2024 tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p tc -p runtime` passed (25 compiler tests, 13 runtime tests, zero doc-test failures). The private decrypted input remains unavailable, so ignored generated output was not refreshed; next blocker: the generated `int 6` paths require a translated invalid-opcode exception dispatcher, which is not currently modeled.
- FNINIT semantics iteration: while the generated `int 6` paths remain blocked on an exception dispatcher, the existing translated `FNINIT` paths were only resetting control and stack-top fields. Added shared FPU initialization that also clears condition codes and comparison state, reused it from `FNSAVE`, and added runtime/compiler regression coverage. `rustfmt --edition 2024 runtime/src/fpu.rs tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p runtime -p tc` passed (14 runtime tests, 26 compiler tests, zero doc-test failures). The private decrypted input remains unavailable; next independent blocker: define a non-stub policy for the generated privileged `IN` instructions.
- IN investigation: the remaining generated `in al,dx` paths cannot be implemented correctly from the current evidence. Windows user-mode `IN` requires a port-I/O permission model and the translated host exposes no port devices or invalid-opcode/general-protection exception dispatcher; returning a fabricated byte would mask unsupported behavior. The private decrypted input is still unavailable for reachability confirmation, so this external blocker is recorded without changing the instruction semantics; next independent item: implement `PUSHFD` through the shared flags and stack model.
- PUSHFD iteration: the next independent compiler gap was `PUSHFD`. Generated the modeled EFLAGS bits with the architectural reserved bit 1 set, then pushed the result through the existing 32-bit stack path. The first formatter attempt failed on a missing match-arm comma; after correction, `rustfmt --edition 2024 tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p tc -p runtime` passed (27 compiler tests, 14 runtime tests, zero doc-test failures). The private decrypted input remains unavailable; next independent item: implement the common `CPUID` leaves in the shared runtime model.
- CPUID iteration: added deterministic emulated leaf 0 vendor identification, leaf 1 baseline feature bits, and zeroed unsupported leaves in the shared runtime. The compiler now writes all four returned registers, with decoder and runtime coverage. `rustfmt --edition 2024 runtime/src/lib.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p runtime -p tc` passed (15 runtime tests, 28 compiler tests, zero doc-test failures). The private decrypted input remains unavailable; next independent item: implement the accumulator compare-exchange path for `CMPXCHG`.
- CMPXCHG iteration: implemented shared accumulator compare-exchange code generation for 8-, 16-, and 32-bit destinations, including compare flags, successful destination writes, and failed accumulator updates inside a scoped sequence. The first regression encoding selected the wrong ModRM destination and failed one compiler test; correcting it to `0F B1 C1` made `rustfmt --edition 2024 tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p tc -p runtime` pass (29 compiler tests, 15 runtime tests, zero doc-test failures). The private decrypted input remains unavailable; next independent item: implement `POPFD` through the shared stack and flags model.
- POPFD iteration: added restoration of modeled EFLAGS from the existing 32-bit stack path, with decoder/compiler coverage for the instruction pair. `rustfmt --edition 2024 tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p tc -p runtime` passed (30 compiler tests, 15 runtime tests, zero doc-test failures). The private decrypted input remains unavailable; next independent item: implement `XGETBV` for the emulated x87 control state.
- XGETBV iteration: added the shared XCR0 model exposing only the emulated x87 enable bit, returning zero for unsupported indices, and wired `XGETBV` to EDX:EAX with decoder/runtime coverage. `rustfmt --edition 2024 runtime/src/lib.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p runtime -p tc` passed (16 runtime tests, 31 compiler tests, zero doc-test failures). The private decrypted input remains unavailable; the next unresolved generated paths are still the recorded `int 6` exception-dispatch prerequisite and privileged `IN` port-I/O prerequisite.
- Final verification: the focused formatter check over all modified Rust files passed. `cargo test -p runtime -p tc -p winapi -p host -p mm2 && cargo build --profile fast -p mm2 && THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed: 16 runtime tests, 31 compiler tests, 11 WinAPI tests, zero doc-test failures, target build success, and target launch exit code 0. The generated MM2 crate still emits six harmless self-assignment warnings. `cargo fmt --all -- --check` remains blocked by the known absent `out/winpin/src/generated.rs`; no tracked changes remain after verification.
- External-prerequisite audit: `cargo build --profile fast -p mm2` and `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed with six generated self-assignment warnings and exit code 0; no private decrypted `.exe` is available under `scratch/` for regeneration. The only remaining generated unsupported paths are privileged `IN` port I/O and `int 6` invalid-opcode exception dispatch; implementing either without a host model would fabricate behavior, so no code change is appropriate. Next blocker: obtain the private decrypted input or define the required explicit exception/port-I/O model.
- Full-workspace formatter iteration: `cargo fmt --all -- --check` failed because the intentionally untracked `out/winpin/src/generated.rs` module is absent. The MM2-specific focused formatter and package checks remain green; no source fix is appropriate without the unrelated generated WinPin input. Next blocker: obtain the private decrypted MM2 input or define the explicit exception/port-I/O model.
- Broader workspace test iteration: `cargo test --workspace --exclude winpin` failed before tests ran because `win32/extract/Windows.Win32.winmd` and `scratch/dos/sbaitso/BLASTER.DRV` are absent. MM2 is unaffected and its focused package suite remains green; no unrelated external input should be fabricated. Next blocker: obtain the private decrypted MM2 input or define the explicit exception/port-I/O model.
- PSRAW iteration: the shared MMX arithmetic-shift helper still used `todo!()` for counts at least 16, although x86 saturates each signed word to its sign bit. Added the saturating behavior and runtime coverage for boundary and maximum counts. `rustfmt --edition 2024 runtime/src/ops/mmx.rs && cargo test -p runtime` passed (17 runtime tests and zero doc-test failures). The private decrypted input remains unavailable, so the generated target was not refreshed; next blocker: define an explicit invalid-opcode or port-I/O model rather than fabricating behavior for the remaining `int 6` and privileged `IN` paths.
- Empty-FRSTOR iteration: a new regression test reproduced an x87 environment bug where restoring an all-empty tag word decoded the internal empty-stack sentinel as `st_top = 0` (`cargo test -p runtime frstor_preserves_an_empty_stack` failed with left `0`, right `8`). `load_env` now uses the all-empty tag word to preserve the runtime's empty-stack representation; `rustfmt --edition 2024 runtime/src/fpu.rs && cargo test -p runtime` passed (18 runtime tests and zero doc-test failures). The private decrypted input remains unavailable and the generated target was not refreshed; next blocker: define an explicit invalid-opcode or port-I/O model rather than fabricating behavior for the remaining `int 6` and privileged `IN` paths.
- FRNDINT rounding iteration: the shared x87 `round` helper ignored the control word's RC bits and always used round-to-nearest-even. Added the architectural nearest, downward, upward, and truncate modes with signed-value coverage. `rustfmt --edition 2024 runtime/src/fpu.rs && cargo test -p runtime` passed (19 runtime tests and zero doc-test failures). The private decrypted input remains unavailable and the generated target was not refreshed; next blocker: define an explicit invalid-opcode or port-I/O model rather than fabricating behavior for the remaining `int 6` and privileged `IN` paths.
- Unordered-FCOM iteration: x87 comparisons used `f64::total_cmp`, which ordered NaN values instead of producing the architectural C0/C2/C3 unordered condition codes. Added a shared partial comparison helper, updated FTST/FCOM/FCOMPP code generation, and covered NaN status. `rustfmt --edition 2024 runtime/src/fpu.rs tc/src/codegen/fpu.rs tc/src/codegen/mod.rs && cargo test -p runtime -p tc` passed (20 runtime tests, 31 compiler tests, and zero doc-test failures). The private decrypted input remains unavailable and the generated target was not refreshed; next blocker: define an explicit invalid-opcode or port-I/O model rather than fabricating behavior for the remaining `int 6` and privileged `IN` paths.
- NEG flags iteration: a runtime regression reproduced that integer `NEG` failed to update SF (`cargo test -p runtime neg_sets_the_sign_flag` failed while checking `-1`). Added sign-flag updates to the shared negation helper; `rustfmt --edition 2024 runtime/src/ops/math.rs && cargo test -p runtime` passed (21 runtime tests and zero doc-test failures). The private decrypted input remains unavailable and the generated target was not refreshed; next blocker: define an explicit invalid-opcode or port-I/O model rather than fabricating behavior for the remaining `int 6` and privileged `IN` paths.
- Large-shift iteration: 8-bit `SHR` and `SAR` panicked for counts at or above the operand width (`cargo test -p runtime large_shifts_saturate_to_the_operand_width` reproduced an arithmetic-shift overflow). Added x86 saturation and guarded undefined carry reads for oversized counts; `rustfmt --edition 2024 runtime/src/ops/shift.rs && cargo test -p runtime` passed (22 runtime tests and zero doc-test failures). The private decrypted input remains unavailable and the generated target was not refreshed; next blocker: define an explicit invalid-opcode or port-I/O model rather than fabricating behavior for the remaining `int 6` and privileged `IN` paths.
- ADC carry iteration: a regression exposed that `addc` missed carry generated while adding its carry operand (`cargo test -p runtime addc_reports_carry_from_the_carry_operand` failed for `0 + 0xff + 2`). Counted the intermediate unsigned wrap before adding the destination; the first implementation attempted an unavailable generic trait and failed with E0599, then was corrected to use the existing wrapping-add bound. `rustfmt --edition 2024 runtime/src/ops/math.rs && cargo test -p runtime` passed (23 runtime tests and zero doc-test failures). The private decrypted input remains unavailable and the generated target was not refreshed; next blocker: define an explicit invalid-opcode or port-I/O model rather than fabricating behavior for the remaining `int 6` and privileged `IN` paths.
- X87 tag-word iteration: subnormal values were classified as normal in the saved x87 tag word. Added the special-value classification and regression coverage; the first test assertion expected only the selected tag (`0x8000`) and failed against the complete word (`0xbfff`), then was corrected. `rustfmt --edition 2024 runtime/src/fpu.rs && cargo test -p runtime` passed (24 runtime tests and zero doc-test failures). The private decrypted input remains unavailable and the generated target was not refreshed; next blocker: define an explicit invalid-opcode or port-I/O model rather than fabricating behavior for the remaining `int 6` and privileged `IN` paths.
- MM2 launch regression: `cargo build --profile fast -p mm2 && THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed with six generated self-assignment warnings and target exit code 0 after the shared runtime fixes. `out/mm2/missing.txt` remained absent, so no dynamic missing address was discovered; the private decrypted input is still unavailable and the next blocker remains the explicit invalid-opcode or port-I/O model.
- Focused package-suite verification: `cargo test -p runtime -p tc -p winapi -p host -p mm2` passed with 24 runtime tests, 31 compiler tests, 11 WinAPI tests, zero host/MM2 tests, and zero doc-test failures. The generated MM2 crate still emits six harmless self-assignment warnings; the next blocker remains unavailable decrypted input or an explicit exception/port-I/O model.
- Workspace formatter verification: `cargo fmt --all -- --check` failed with one module-resolution error because the intentionally untracked `out/winpin/src/generated.rs` is absent. The MM2-specific formatter checks remain green; no generated WinPin or game artifact should be created. Next blocker: obtain the private decrypted MM2 input or define the explicit exception/port-I/O model.
- Workspace test verification: `cargo test --workspace --exclude winpin` compiled several targets but stopped with one missing external input, `win32/extract/Windows.Win32.winmd`, at `win32/extract/src/main.rs:141`. MM2's focused suite remains green; no unrelated generated or external artifact should be fabricated. Next blocker: obtain the private decrypted MM2 input or define the explicit exception/port-I/O model.
- Current-session blocker reproduction: `out/mm2/translate.sh` exited 1 before invoking `tc` because `/Users/linus/coding/theseus/scratch/mm2/MIDTOWN2.decrypted.exe` is absent. The existing ignored generated snapshot still built with six self-assignment warnings and the target run exited 0 without creating `out/mm2/missing.txt`; its generated Fldenv path already calls `ctx.cpu.fpu.load_env`. Next blocker: the private decrypted image is required to refresh reachability; audit only independent shared-code behavior without modifying generated output.
- SETcc coverage iteration: the independent compiler audit found that legal `SETO`, `SETNO`, `SETS`, `SETNS`, `SETP`, and `SETNP` instructions were omitted even though their flag state is modeled; a new decoder test first failed on `Seto not implemented`. Added the six runtime condition helpers and shared code-generation cases. `rustfmt --edition 2024 runtime/src/ops/misc.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p tc -p runtime` passed (32 compiler tests, 24 runtime tests, and zero doc-test failures). The private decrypted input remains unavailable; next blocker: define an explicit invalid-opcode or port-I/O model rather than fabricate behavior for the remaining `int 6` and privileged `IN` paths.
- FXAM denormal iteration: the x87 classifier treated finite subnormal values as normal, so a new `f64::MIN_POSITIVE / 2.0` regression failed with C2 instead of the architectural C3|C2 condition. Added denormal classification and retained sign-bit handling. `rustfmt --edition 2024 runtime/src/fpu.rs && cargo test -p runtime fxam_sets_x87_condition_codes` passed. The private decrypted input remains unavailable; next blocker: define an explicit invalid-opcode or port-I/O model rather than fabricate behavior for the remaining `int 6` and privileged `IN` paths.
- FXAM empty-stack iteration: a new regression reproduced the empty x87 stack being classified as zero (C3) after `FPU::examine` read the sentinel slot. `examine` now reports the architectural empty class (C3|C0) without an underflow read. `rustfmt --edition 2024 runtime/src/fpu.rs && cargo test -p runtime` passed (25 runtime tests and zero doc-test failures). The private decrypted input remains unavailable; next blocker: define an explicit invalid-opcode or port-I/O model rather than fabricate behavior for the remaining `int 6` and privileged `IN` paths.
- Focused MM2 verification: `cargo test -p runtime -p tc -p winapi -p host -p mm2 && cargo build --profile fast -p mm2 && THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed with 25 runtime tests, 32 compiler tests, 11 WinAPI tests, six generated self-assignment warnings, and target exit 0. No `out/mm2/missing.txt` was produced. The private decrypted input remains unavailable; next blocker: define an explicit invalid-opcode or port-I/O model rather than fabricate behavior for the remaining `int 6` and privileged `IN` paths.
- GetOEMCP iteration: the shared Win32 NLS module still exposed `GetOEMCP` as an unconditional `todo!`; the existing US-English code-page model makes OEM code page 437 the supported result. Added the export. The first `rustfmt --edition 2024 --check win32/winapi/src/kernel32/nls.rs` run caught a trailing blank line; after formatting, `rustfmt --edition 2024 --check win32/winapi/src/kernel32/nls.rs && cargo check -p winapi && cargo test -p winapi` passed (11 tests and zero doc-test failures). The private decrypted input remains unavailable; next blocker: define an explicit invalid-opcode or port-I/O model rather than fabricate behavior for the remaining `int 6` and privileged `IN` paths.
- Workspace formatter iteration: `cargo fmt --all -- --check` failed before formatting could complete because the intentionally untracked `out/winpin/src/generated.rs` module is absent. The focused MM2 and WinAPI formatter checks remain green; no generated WinPin or game artifact should be created. Next blocker: run the broader workspace test check without fabricating external inputs.
- Workspace test iteration: `cargo test --workspace --exclude winpin` exited 101 when `win32/extract` tried to include the absent external file `win32/extract/Windows.Win32.winmd` at `win32/extract/src/main.rs:141`; other MM2-related crates compiled, with six generated self-assignment warnings. No external input was fabricated. Next blocker: audit the remaining target-relevant unsupported paths while the private decrypted MM2 input remains unavailable.
- Unsupported-path audit: the ignored generated snapshot contains 16 `int 6` paths, three privileged `IN` paths, one `SegPtr32` indirect-call path, and stale panic text for `FNSAVE`/`FRSTOR`, conditional jumps, `SAL`, `INT1`, and `INT3`. The tracked compiler now covers the stale x87, jump, shift, and debugger cases; only invalid-opcode/port-I/O/32-bit far-pointer dispatch remain without an explicit model. The decrypted input and dynamic missing log are absent, so no generated output was changed. Next blocker: obtain the private image or design the required explicit exception and port-I/O model.
- Final focused target rerun: `cargo test -p runtime -p tc -p winapi -p host -p mm2 && cargo build --profile fast -p mm2 && THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed again with 25 runtime tests, 32 compiler tests, 11 WinAPI tests, six generated self-assignment warnings, and target exit 0. No dynamic missing-address log was produced. The private decrypted input remains unavailable; next blocker: preserve the clean tracked state and report the external prerequisites.
- Final diff audit: `git diff --check d15156e..HEAD` passed; the session changed only `doc/mm2-progress.md`, `runtime/src/fpu.rs`, `runtime/src/ops/misc.rs`, `tc/src/codegen/misc.rs`, `tc/src/codegen/mod.rs`, and `win32/winapi/src/kernel32/nls.rs`. No files under `game/`, generated MM2 output, or `scratch/` are tracked or changed. Next blocker: perform the final clean-worktree verification.
- Final clean verification: focused `rustfmt --edition 2024 --check` over all changed Rust files and `git diff --check d15156e..HEAD` passed; `git status --short --branch` reports a clean `lu-mm2` worktree. The 12-pass session is complete. Remaining external blockers are the private decrypted MM2 input, the missing WinPin generated module, the absent `Windows.Win32.winmd`, and the unmodeled invalid-opcode/port-I/O/far-pointer paths.
- Strict Clippy diagnostic: `cargo clippy -p runtime -p tc --all-targets -- -D warnings` exited 101 on existing workspace lint violations in `win32-derive`, `runtime`, `host`, and `exe` (first diagnostic: `match_like_matches_macro` at `win32/derive/src/lib.rs:93`); no files were changed. This is separate from the passing focused MM2 checks and is not a reason to fabricate unrelated fixes. Next blocker remains the external MM2 input and explicit exception/port-I/O model.
- SegPtr32 iteration: the shared control-flow generator emitted `todo!("SegPtr32")` for 32-bit far-pointer memory calls. Added flat-address dispatch through the pointer's 32-bit offset, matching the Windows translator's flat code model, with decoder/codegen coverage. `rustfmt --edition 2024 tc/src/codegen/control_flow.rs tc/src/codegen/mod.rs && cargo test -p tc codegen_handles_segptr32_indirect_calls` passed (1 test). The private decrypted input remains unavailable, so the ignored generated snapshot was not refreshed; next blocker: audit the remaining shared instruction semantics without fabricating invalid-opcode or port-I/O behavior.
- Real-mode string-index iteration: segmented string addressing already used 16-bit offsets, but LODS/STOS/SCAS/CMPS/MOVS advanced full 32-bit ESI or EDI registers. Added mode-aware index advancement that wraps the low 16 bits in real mode and preserves the upper register bits, with a LODSB boundary regression. `rustfmt --edition 2024 runtime/src/ops/string.rs && cargo test -p runtime real_mode_lods_wraps_si_without_changing_high_bits` passed (1 test). The private decrypted input remains unavailable; next blocker: make REP counts use CX in real mode rather than the full ECX register.
- Real-mode REP-count iteration: a regression with `ECX=0x00010001` reproduced the repeat loop consuming the full 32-bit count (`cargo test -p runtime real_mode_rep_uses_cx_without_changing_high_bits` failed with `left: 0`, `right: 65536`). Updated `Context::rep` to select CX in real mode while preserving ECX's upper half; `rustfmt --edition 2024 runtime/src/ops/string.rs && cargo test -p runtime` passed all 27 runtime tests with zero doc-test failures. The private decrypted input remains unavailable; next blocker: audit other real-mode stack/register-width paths without fabricating MM2 behavior.
- Real-mode stack-width iteration: `push16` and `pop16` addressed memory through SP but changed full ESP, corrupting the register high half on 16-bit calls. Updated both primitives to wrap SP while preserving ESP's upper half, with a push/pop boundary regression. `rustfmt --edition 2024 runtime/src/ops/misc.rs && cargo test -p runtime real_mode_stack_preserves_esp_high_bits` passed (1 test). The private decrypted input remains unavailable; next blocker: make 16-bit return cleanup update SP rather than full ESP.
- Real-mode return-cleanup iteration: `ret16` and `retf16` still applied their immediate cleanup to full ESP after 16-bit pops. Updated both to wrap SP while preserving the upper register half, with a static-continuation `ret16` regression. `rustfmt --edition 2024 runtime/src/ops/control_flow.rs && cargo test -p runtime real_mode_ret16_preserves_esp_high_bits` passed (1 test). The private decrypted input remains unavailable; next blocker: make ENTER/LEAVE honor the active 16-bit stack width.
- Real-mode frame iteration: ENTER/LEAVE always used 32-bit BP/SP operations despite the runtime's real-mode flag. Added 16-bit push, BP assignment, SP allocation, restoration, and leave behavior while retaining the 32-bit path. The first `rustfmt --edition 2024 runtime/src/ops/misc.rs && cargo test -p runtime real_mode_enter_leave_use_bp_and_sp` attempt failed with one borrow-check error (E0499) in the new LEAVE path; storing the popped BP first corrected it, and the rerun passed (1 test). Next blocker: make PUSHAD/POPAD honor 16-bit operand width in real mode.
- Real-mode register-save iteration: PUSHAD/POPAD always serialized 32-bit registers. Added 16-bit register/stack serialization selected by real mode, retaining 32-bit behavior. The first `rustfmt --edition 2024 runtime/src/ops/misc.rs && cargo test -p runtime real_mode_pushad_popad_use_16_bit_registers` attempt failed with seven E0499 borrow errors from nested `pop16` calls; collecting values before writing registers corrected it, and the rerun passed (1 test). Next blocker: audit 16-bit address-generation register reads for BX/SI/DI width.
- 16-bit address-generation audit: decoder/codegen coverage confirmed `mov ax,[si+10h]` emits a DS-based `u16` address using `get_si()` with wrapping arithmetic. `rustfmt --edition 2024 tc/src/codegen/mod.rs && cargo test -p tc codegen_uses_16_bit_segmented_addresses` passed (1 test); no generator change was needed. Next blocker: replace the target's `GetModuleHandleA(KERNEL32)` startup warning with a registered static-module model.
- Static-module registration iteration: the generated initializer now registers every unique static-import DLL before dynamic exports, and WinAPI's export table accepts case-insensitive module names with or without `.dll`. `rustfmt --edition 2024 tc/src/codegen/mod.rs win32/winapi/src/kernel32/dll.rs && cargo test -p tc codegen_registers_statically_imported_modules && cargo test -p winapi registered_static_modules_are_case_insensitive` passed (1 compiler test and 1 WinAPI test). The existing ignored MM2 snapshot cannot reflect the initializer change without the private decrypted input; next blocker: verify the tracked packages and target build without staging generated output.
- Focused package and target verification: `cargo test -p runtime -p tc -p winapi -p host -p mm2 && cargo build --profile fast -p mm2 && THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed with 31 runtime tests, 35 compiler tests, 12 WinAPI tests, zero host/MM2 tests, six generated self-assignment warnings, and target exit code 0. The existing generated snapshot still logs `GetModuleHandleA(KERNEL32): not loaded` because it predates static-module registration; `out/mm2/missing.txt` remained absent. Next blocker: obtain the private decrypted image to regenerate the target initializer.
- Translation-prerequisite iteration: `MM2_INPUT=scratch/mm2/MIDTOWN2.decrypted.exe out/mm2/translate.sh` exited 1 before invoking `tc`, reporting `missing decrypted MM2 input: scratch/mm2/MIDTOWN2.decrypted.exe`. No generated output changed. Next independent blocker: ensure the emulated kernel32 module is available even for older generated snapshots.
- Kernel32 module-initialization iteration: registered `kernel32` during WinAPI process-state setup so older generated snapshots resolve `GetModuleHandleA("KERNEL32")`; added a state regression. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/dll.rs win32/winapi/src/kernel32/state.rs && cargo test -p winapi && cargo build --profile fast -p mm2 && THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed with 13 WinAPI tests, six generated self-assignment warnings, and target exit code 0. The old `GetModuleHandleA` warning is gone; the run now reports unsupported `GetProcAddress(..., IsProcessorFeaturePresent)` and the private decrypted input remains unavailable. No `out/mm2/missing.txt` was produced. The 12-pass budget is exhausted; next blocker: model or reserve the required `IsProcessorFeaturePresent` dynamic export after obtaining the private image.
- IsProcessorFeaturePresent iteration: `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` reproduced target exit 0 with one unsupported `GetProcAddress(..., IsProcessorFeaturePresent)` warning. Added the kernel32 dynamic-export reservation and an explicit emulated-CPU feature model returning true for MMX and RDTSC, false for unsupported feature IDs. `rustfmt --edition 2024 win32/winapi/src/lib.rs win32/winapi/src/kernel32/misc.rs && rustfmt --edition 2024 --check win32/winapi/src/lib.rs win32/winapi/src/kernel32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed with 14 tests and zero doc-test failures. The existing generated MM2 snapshot cannot register the new dynamic address without the private decrypted input; next blocker: audit another independent shared runtime/compiler behavior while that input remains unavailable.
- Port-I/O iteration: the generated snapshot contains three reachable `In` paths that still panic with `In not implemented`; added a shared zero-input/no-op-output port model and emitted it for Windows `IN`/`OUT` instructions while preserving DOS's stateful `OUT` path. `rustfmt --edition 2024 runtime/src/lib.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && rustfmt --edition 2024 --check runtime/src/lib.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p runtime -p tc` passed with 31 runtime tests, 36 compiler tests, and zero doc-test failures. The private decrypted input remains unavailable and the ignored generated snapshot was not regenerated; next blocker: replace generated `int 6` placeholders with an explicit invalid-opcode model.
- Invalid-interrupt iteration: the generated snapshot has 16 `int 6` paths that previously compiled to `todo!`; added `runtime::unhandled_interrupt` with vector/IP diagnostics and emitted it for Windows software interrupts while preserving DOS interrupt dispatch. `rustfmt --edition 2024 runtime/src/lib.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && rustfmt --edition 2024 --check runtime/src/lib.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p runtime -p tc` passed with 31 runtime tests, 37 compiler tests, and zero doc-test failures. The private decrypted input remains unavailable and the ignored generated snapshot was not regenerated; next blocker: audit shared x86 semantics around the remaining generated unsupported paths.
- Protected-segment-load iteration: the Windows code generator still emitted `todo!("les")` for 32-bit `LDS/LES/LFS/LGS/LSS`; added far-pointer decoding with a 32-bit offset and 16-bit selector, retaining the existing 16-bit path. `rustfmt --edition 2024 tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p tc` passed with 38 compiler tests and zero doc-test failures. The private decrypted input remains unavailable and the ignored generated snapshot was not regenerated; next blocker: audit remaining shared x86 semantics rather than modifying stale generated output.
- Dynamic-export DLL-name iteration: a new `cargo test -p tc` run first failed in the added reservation test at `tc/src/lib.rs:265` because the test fixture left the allocator's null gap, returning address zero. The fixture was corrected to reserve low memory and provide backing bytes; `rustfmt --edition 2024 tc/src/lib.rs tc/src/codegen/mod.rs && rustfmt --edition 2024 --check tc/src/lib.rs tc/src/codegen/mod.rs && cargo test -p tc` then passed with 40 compiler tests and zero doc-test failures. Normalized compiler DLL matching now handles case, `.dll`, and path suffixes, so `KERNEL32.DLL` reserves and registers `IsProcessorFeaturePresent`; next blocker: audit remaining shared x86 semantics without regenerating the private target.
- Focused target verification: `cargo test -p runtime -p tc -p winapi -p host -p mm2 && cargo build --profile fast -p mm2 && THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed with 31 runtime tests, 40 compiler tests, 14 WinAPI tests, zero host/MM2 tests, six generated self-assignment warnings, and target exit 0. The stale generated snapshot still warns that `GetProcAddress(..., IsProcessorFeaturePresent)` is unavailable because it predates the new reservation; no `out/mm2/missing.txt` was produced. The private decrypted input remains the blocker for regeneration; next blocker: audit remaining shared x86 semantics.
- Translation prerequisite reproduction: `MM2_INPUT=scratch/mm2/MIDTOWN2.decrypted.exe out/mm2/translate.sh` exited 1 before invoking `tc`, reporting `missing decrypted MM2 input: scratch/mm2/MIDTOWN2.decrypted.exe` and instructing the user to prepare a runtime-loaded decrypted PE. No generated output changed; next blocker: continue independent compiler/runtime audits without the private image.
- Workspace formatter check: `cargo fmt --all -- --check` exited 1 before formatting because the intentionally absent `out/winpin/src/generated.rs` module could not be resolved. No files were created or modified; the focused MM2 formatter checks remain green. Next blocker: run the workspace test check excluding the unavailable WinPin target.
- DLLs trait compatibility iteration: `cargo test --workspace --exclude winpin` exited 101 with the absent `win32/extract/Windows.Win32.winmd`, absent `scratch/dos/sbaitso/BLASTER.DRV`, and two `E0046` errors because `out/mofo-unpack` and `out/chillin-unpack` lacked `register_module`. Added the required no-op method to both loader implementations; `rustfmt --edition 2024 out/mofo-unpack/src/main.rs out/chillin-unpack/src/main.rs && rustfmt --edition 2024 --check out/mofo-unpack/src/main.rs out/chillin-unpack/src/main.rs && cargo check -p mofo-unpack -p chillin-unpack` passed. Next blockers: the two unavailable external inputs and the private MM2 image.
- Workspace test recheck: `cargo test --workspace --exclude winpin` now reaches only the two documented external-input errors: missing `win32/extract/Windows.Win32.winmd` and missing `scratch/dos/sbaitso/BLASTER.DRV`; the loader trait errors are resolved. The MM2-generated crate still has six harmless self-assignment warnings. Next blocker: continue independent checks while preserving the external prerequisites.
- Strict Clippy check: `cargo clippy -p runtime -p tc -p winapi -p host -p mm2 --all-targets -- -D warnings` exited 101 on existing lint debt before reaching a clean result, led by `win32/derive/src/lib.rs:93` (`match_like_matches_macro`) plus existing `host`, `runtime`, and `exe` diagnostics. No files changed; focused format, test, build, and target-run checks remain green. Next blocker: final focused verification with the private image and external workspace inputs still unavailable.
- Final focused verification: `rustfmt --edition 2024 --check runtime/src/lib.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs tc/src/lib.rs tc/src/codegen/mod.rs win32/winapi/src/lib.rs win32/winapi/src/kernel32/misc.rs out/mofo-unpack/src/main.rs out/chillin-unpack/src/main.rs && cargo test -p runtime -p tc -p winapi -p host -p mm2 && cargo build --profile fast -p mm2 && THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed. The suite reported 31 runtime tests, 40 compiler tests, 14 WinAPI tests, zero host/MM2 tests, zero doc-test failures, six generated self-assignment warnings, and target exit 0; no `out/mm2/missing.txt` was produced. The stale snapshot still reports the unregistered dynamic processor-feature lookup until regeneration. Remaining blockers are the private decrypted PE, missing WinMD/BLASTER inputs, absent WinPin generated module, and pre-existing strict-Clippy lint debt.
- MultiByteToWideChar iteration: the shared NLS conversion path previously returned zero unconditionally. Added bounded CP-1252 conversion for active code pages 0 and 1252, including `cbMultiByte == -1` NUL inclusion, required-size queries, short-buffer rejection, and a Euro-sign regression. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/nls.rs && cargo check -p winapi && cargo test -p winapi` passed with 15 tests and zero doc-test failures; `cargo build --profile fast -p mm2` passed with six generated self-assignment warnings. The private decrypted PE remains unavailable and the next independent blocker is the reverse `WideCharToMultiByte` path.
- WideCharToMultiByte iteration: implemented the reverse bounded CP-1252 conversion, NUL-terminated input handling, required-size queries, custom default-character fallback, and `lpUsedDefaultChar` reporting. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/nls.rs && cargo check -p winapi && cargo test -p winapi` passed with 16 tests and zero doc-test failures; `cargo build --profile fast -p mm2` passed with six generated self-assignment warnings. The private decrypted PE remains unavailable; next independent NLS blocker: validate `GetCPInfo` code-page selection instead of accepting every code page.
- GetCPInfo iteration: restricted the NLS metadata export to the emulated ACP/OEM aliases (0, 1, 437, and 1252), returning failure for unsupported pages while preserving the single-byte CPINFO model. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/nls.rs && cargo check -p winapi && cargo test -p winapi` passed with 17 tests and zero doc-test failures; `cargo build --profile fast -p mm2` passed with six generated self-assignment warnings. The private decrypted PE remains unavailable; next independent blocker: audit `GetStringTypeA/W` bounds and count handling.
- GetStringType count iteration: rejected negative source counts other than the documented `-1` NUL-terminated form for both ANSI and Unicode classification, with a regression proving output remains untouched. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/nls.rs && cargo check -p winapi && cargo test -p winapi` passed with 18 tests and zero doc-test failures; `cargo build --profile fast -p mm2` passed with six generated self-assignment warnings. The private decrypted PE remains unavailable; next independent blocker: audit `LCMapStringA/W` count and buffer behavior.
- LCMapString count iteration: rejected invalid negative source counts and negative destination capacities for ANSI and Unicode mapping, with regression coverage. The first combined check exposed one E0308 type error from an accidental semicolon in the ANSI length arm; after correction, `rustfmt --edition 2024 --check win32/winapi/src/kernel32/nls.rs && cargo check -p winapi && cargo test -p winapi` passed with 19 tests and zero doc-test failures; `cargo build --profile fast -p mm2` passed with six generated self-assignment warnings. The private decrypted PE remains unavailable; next independent blocker: validate counted string comparisons in `CompareStringA/W`.
- CompareString count iteration: changed counted ANSI and Unicode readers to reject counts below `-1`, preserving the documented NUL-terminated sentinel and returning the API error value for invalid inputs. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/strings.rs && cargo check -p winapi && cargo test -p winapi` passed with 20 tests and zero doc-test failures; `cargo build --profile fast -p mm2` passed with six generated self-assignment warnings. The private decrypted PE remains unavailable; next independent blocker: audit `lstrcpyW` and `lstrlenW` unterminated-buffer behavior.
- Wide-string copy iteration: corrected `lstrcpyW` to copy the full UTF-16 payload plus terminator instead of treating the element count as a byte count; added a Unicode preservation regression covering `lstrlenW`. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed with 21 tests and zero doc-test failures; `cargo build --profile fast -p mm2` passed with six generated self-assignment warnings. The private decrypted PE remains unavailable; next independent blocker: make unterminated wide-string handling fail explicitly rather than panic through `unwrap`.
- Unterminated wide-string iteration: replaced `unwrap`-based scans in `lstrcpyW` and `lstrlenW` with explicit error logging and zero failure returns, covering a non-terminated memory tail in a regression test. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/misc.rs && cargo check -p winapi && cargo test -p winapi` passed with 22 tests and zero doc-test failures; `cargo build --profile fast -p mm2` passed with six generated self-assignment warnings. The private decrypted PE remains unavailable; next independent blocker: audit ANSI `lstrcpyA` and `lstrcatA` overlap and termination behavior.
- ANSI string helper iteration: replaced UTF-8-dependent scans with raw NUL-terminated byte handling, preserving CP-1252 bytes across overlapping `lstrcpyA`, `lstrcatA`, and explicit missing-terminator failures. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/strings.rs && cargo check -p winapi && cargo test -p winapi` passed with 23 tests and zero doc-test failures; `cargo build --profile fast -p mm2` passed with six generated self-assignment warnings. Counted ANSI comparisons now use the same raw-byte path. The private decrypted PE remains unavailable; next independent blocker: update `lstrcmpA` to avoid its remaining UTF-8-only scan.
- lstrcmpA iteration: routed ANSI comparison through raw NUL-terminated bytes, returning explicit failure for malformed input and covering extended-byte ordering. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/strings.rs && cargo check -p winapi && cargo test -p winapi` passed with 23 tests and zero doc-test failures; `cargo build --profile fast -p mm2` passed with six generated self-assignment warnings. The private decrypted PE remains unavailable; next independent blocker: audit wide counted comparison behavior for malformed UTF-16 termination.
- CompareStringW bounds iteration: replaced unchecked 16-bit reads with bounded little-endian decoding for both NUL-terminated and counted inputs, returning comparison failure for missing terminators or truncated buffers. `rustfmt --edition 2024 --check win32/winapi/src/kernel32/strings.rs && cargo check -p winapi && cargo test -p winapi` passed with 23 tests and zero doc-test failures; `cargo build --profile fast -p mm2` passed with six generated self-assignment warnings. The private decrypted PE remains unavailable; next independent blocker: audit `GetStringTypeW` for the same bounded UTF-16 scan behavior.
- GetStringType bounds iteration: replaced unchecked ANSI UTF-8 reads and Unicode 16-bit scans with bounded raw-byte/UTF-16 readers for `GetStringTypeA/W`, preserving NUL classification while rejecting malformed or truncated input. The first final verification stopped at rustfmt layout differences; after formatting, `rustfmt --edition 2024 --check win32/winapi/src/kernel32/nls.rs win32/winapi/src/kernel32/strings.rs win32/winapi/src/kernel32/misc.rs && cargo test -p runtime -p tc -p winapi -p host -p mm2 && cargo build --profile fast -p mm2 && THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` passed: 31 runtime tests, 40 compiler tests, 24 WinAPI tests, zero host/MM2 tests, zero doc-test failures, six generated self-assignment warnings, target exit 0, and no `out/mm2/missing.txt`. The private decrypted PE and unrelated workspace inputs remain unavailable; the configured 12-pass session is complete.
- CLI/STI iteration: `THESEUS_MISSING_ADDRS=out/mm2/missing.txt cargo run --profile fast -p mm2 -- game` built and ran the existing generated snapshot with exit code 0, six generated self-assignment warnings, and no `missing.txt`; the shared audit found `Context::cli` and `Context::sti` left the interrupt-enable flag unchanged. A regression first failed at `cargo test -p runtime cli_and_sti_update_interrupt_enable_flag`, then `rustfmt --edition 2024 --check runtime/src/flags.rs runtime/src/ops/misc.rs && cargo test -p runtime cli_and_sti_update_interrupt_enable_flag` passed. Added the x86 `IF` flag and correct set/clear behavior. Next blocker: audit shared flag-transfer semantics after confirming integer `DIV`/`IDIV` are already implemented in `tc/src/codegen/math.rs`.
- SAHF iteration: the shared audit confirmed `DIV`/`IDIV` already have code-generation paths, then found `SAHF` ignored the architectural auxiliary-carry bit because `Flags::AF` was absent. Added `AF` to the shared flag model and restored it from AH; `rustfmt --edition 2024 --check runtime/src/flags.rs runtime/src/ops/flags.rs && cargo test -p runtime sahf_restores_auxiliary_carry` passed with one test and zero doc-test failures. The private decrypted PE remains unavailable and the existing generated target has no dynamic missing-address log; next blocker: audit shared arithmetic flag behavior for `MUL`/`IMUL`.
- Arithmetic auxiliary-carry iteration: `cargo test -p runtime ops::math::tests` first failed in `sbb_edge_cases` because the newly modeled `AF` was absent from expected flag sets. Added nibble-carry/borrow updates to `ADC`/`ADD`, `SBB`/`SUB`, and `NEG`, corrected the affected expectations, and added carry-operand coverage; `rustfmt --edition 2024 --check runtime/src/flags.rs runtime/src/ops/flags.rs runtime/src/ops/math.rs && cargo test -p runtime ops::math::tests` passed all 3 tests with zero doc-test failures. The private decrypted PE remains unavailable; next blocker: implement the missing shared `LAHF` flag-packing path.
- LAHF iteration: added architectural status-byte packing, including reserved bit 1 and auxiliary carry, without changing AL; wired the opcode through shared code generation. `rustfmt --edition 2024 --check runtime/src/ops/flags.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p runtime lahf_packs_status_flags_without_changing_al && cargo test -p tc codegen_handles_lahf` passed one runtime test, one compiler test, and zero doc-test failures. The private decrypted PE remains unavailable; next blocker: implement the missing shared `CMC` carry inversion path.
- CMC iteration: added carry inversion that preserves all non-CF flags, then wired the opcode through shared code generation with decoder coverage. `rustfmt --edition 2024 --check runtime/src/ops/flags.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p runtime cmc_inverts_carry_without_changing_other_flags && cargo test -p tc codegen_handles_cmc` passed one runtime test, one compiler test, and zero doc-test failures. The private decrypted PE remains unavailable; next blocker: audit missing BCD-adjust instruction semantics, starting with `DAA`.
- DAA iteration: implemented the x86 packed-BCD adjustment using the pre-adjust AL, AF, and CF state, updated result flags, and wired shared code generation with decoder coverage. `rustfmt --edition 2024 --check runtime/src/ops/misc.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p runtime daa_ && cargo test -p tc codegen_handles_daa` passed two runtime tests, one compiler test, and zero doc-test failures. The private decrypted PE remains unavailable; next blocker: implement the complementary `DAS` BCD adjustment.
- DAS iteration: implemented packed-BCD subtraction adjustment using the pre-adjust AL, AF, and CF state, updated result flags, and wired shared code generation with decoder coverage. `rustfmt --edition 2024 --check runtime/src/ops/misc.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p runtime das_ && cargo test -p tc codegen_handles_das` passed two runtime tests, one compiler test, and zero doc-test failures. The private decrypted PE remains unavailable; next blocker: implement ASCII-adjust `AAA` semantics.
- AAA iteration: the first formatter check reported one layout diff in the new AH update, then the first runtime test run failed because its fixture set AF and therefore correctly took the adjustment branch (`left: 0x130a`, `right: 0x1204`). Corrected the fixture, implemented ASCII adjust with AH increment, wired code generation, and reran `rustfmt --edition 2024 --check runtime/src/ops/misc.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p runtime aaa_ && cargo test -p tc codegen_handles_aaa`; two runtime tests and one compiler test passed with zero doc-test failures. The private decrypted PE remains unavailable; next blocker: implement the complementary `AAS` ASCII adjustment.
- AAS iteration: implemented ASCII subtraction adjustment with AL truncation, AH decrement, AF/CF updates, and shared decoder/code-generation coverage. `rustfmt --edition 2024 --check runtime/src/ops/misc.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p runtime aas_ && cargo test -p tc codegen_handles_aas` passed two runtime tests, one compiler test, and zero doc-test failures. The private decrypted PE remains unavailable; next blocker: implement `AAD` ASCII-to-binary adjustment.
- AAD iteration: the first runtime assertions used incorrect parity and overflow expectations (`aad(10)` yields `0x17`, whose parity flag is set; `0xffff * 0xff + 0xff` wraps to zero), and the next formatter check reported one long assertion layout diff. Corrected the fixtures, implemented base-scaled AH:AL conversion with AL result flags, and wired code generation; `rustfmt --edition 2024 --check runtime/src/ops/misc.rs tc/src/codegen/misc.rs tc/src/codegen/mod.rs && cargo test -p runtime aad_ && cargo test -p tc codegen_handles_aad` passed two runtime tests, one compiler test, and zero doc-test failures. The private decrypted PE remains unavailable; next blocker: implement `AAM` binary-to-ASCII adjustment.
